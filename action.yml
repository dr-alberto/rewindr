name: rewindr

inputs:
  github-token:
    description: "The GITHUB_TOKEN to use for uploading artifacts"
    required: true
    default: ${{ github.token }}
runs:
  using: "node24"
  main: "src/main.js"
  post: "src/post.js"
  post-if: failure()
